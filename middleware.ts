// Denne fil er udfaset — Next.js 16 omdøbte konventionen fra
// "middleware.ts" til "proxy.ts" (se ./proxy.ts, hvor al logikken nu bor).
// Filen kan ikke slettes fra dette miljø, men er bevidst tom: at have
// både middleware.ts og proxy.ts med kode i får Next.js' build til at
// fejle ("Both middleware file and proxy file are detected").
